<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project1mp"> <!-- Perbaikan: Package sesuai proyek -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project1MP"> <!-- Asumsi theme sesuai proyek, sesuaikan jika perlu. TUTUP TAG DI SINI -->

        <!-- Komentar yang menjelaskan android:label bisa diletakkan di sini jika perlu -->
        <!-- Akan diisi "Project 1 MP" di strings.xml -->

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Perbaikan: Tambah deklarasi LayoutPracticeActivity -->
        <activity
            android:name=".LayoutPracticeActivity"
            android:exported="false" /> <!-- Tidak perlu intent-filter karena bukan launcher -->

        <!-- Other components like services, receivers, providers -->

    </application>

</manifest>
